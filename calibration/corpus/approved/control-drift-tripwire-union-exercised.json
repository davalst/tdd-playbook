{
  "id": "control-drift-tripwire-union-exercised",
  "agent": "tripwire-auditor",
  "plant": "Same INC-42 tripwire region, but the loop iterates the UNION of both copies, so a name present in only one copy \u2014 the motivating pre-fix drift \u2014 fails the agreement assertion in either direction; the loop-over-a-set-expression shape looks like the intersection trick but is genuinely exercised, and the auditor must certify it.",
  "control_for": "drift-tripwire-intersection-excuse",
  "edits": [
    {
      "file": "tests/test_tools.py",
      "old": "    def test_two_copies_agree_today(self):\n        self.assertEqual(set(READ_ONLY_TOOLS), set(_READ_ONLY))",
      "new": "    def test_two_copies_agree_today(self):\n        # INC-42 drift tripwire: 'status' was added to tools.READ_ONLY_TOOLS only\n        # (pre-fix shape: READ_ONLY_TOOLS = (\"view\", \"list\", \"search\", \"status\") with\n        # audit._READ_ONLY unchanged), so the CLI skipped the write-lock while audit\n        # tagged the same call mutating. Guard: a name in EITHER copy must classify\n        # identically in both \u2014 a name present in only one copy is the pre-fix drift.\n        for name in set(READ_ONLY_TOOLS) | set(_READ_ONLY):\n            self.assertEqual(is_read_only(name), not mutating(name))"
    }
  ],
  "task": "The INC-42 drift tripwire shipped as test_two_copies_agree_today in tests/test_tools.py, built after a read-only name was added to one copy of the list only (the pre-fix shape in its comment). Audit this deliverable through the Tripwire legs (BUILT + WIRED + ACTIVATED + EXERCISED); per \u00a713 the EXERCISED leg requires the motivating defect shape to be among the inputs the guard can actually go RED on. Report each leg and the overall verdict.",
  "must_match": [
    "(?i)(union|either|combined)",
    "(?i)((catch(es)?|caught|detect(s|ed)?|fail(s|ed)?|goes? red|would (go )?red|red)[^\\n]{0,60}(drift|status|one (copy|list)|only one|motivating)|(drift|status|one (copy|list)|only one|motivating)[^\\n]{0,60}(fail|red|caught|catch|detect))",
    "(?i)exercised"
  ],
  "must_not_match": [
    "(?i)(never|not) exercised",
    "(?i)(guard|tripwire|test) excuses",
    "(?i)exercised[^a-zA-Z\\n]{0,4}(no|fail)",
    "(?i)stays? green[^\\n]{0,40}(drift|status|one (copy|list))"
  ],
  "_meta": {
    "authored_by_model": "claude-fable-5",
    "authored_at": "2026-08-04",
    "status": "approved",
    "approved_at": "2026-08-04"
  }
}
